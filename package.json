{
  "name": "replace-ssh-to-token",
  "version": "0.1.0",
  "description": "Install NPM package hosted by private Bitbucket repo.",
  "repository": {
    "type": "git",
    "url": "https://github.com/bandit-hire5/replace-ssh-to-token"
  },
  "bin": "index.js",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
